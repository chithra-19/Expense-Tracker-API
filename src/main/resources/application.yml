server:
  port: ${PORT:8081}

spring:
  application:
    name: expense-tracker

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/expense_tracker?serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Chithra@2619}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

jwt:
  secret: ${JWT_SECRET:O5oxf0al1FvzdNZqg2sSbLbBbLSbpPOeVQy+wNdDgSw=}
  expiration: 86400000
  header: Authorization
  prefix: Bearer

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG